enum Attack{
   PAUSE, //Pause until space bar
   LASER, //Spawn a laser
   FRAMES, //Wait n frames
   TRAIN, //Spawn a train
   SEL, //add n objects to selection
   FREEZE //Set dy + dx of n objects in selection to 0
}
